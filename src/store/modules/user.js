const state = {
  info: {}
}

const getters = {}

const mutations = {
  // 设置用户信息
  SET_USER_INFO (state, data) {
    state.info = data
  }
}

const actions = {}

export default {
  state,
  getters,
  mutations,
  actions
}
